{
  "name": "@eventvisor/project-no-environments",
  "private": true,
  "version": "1.0.0",
  "description": "Starter Eventvisor project without sets or release environments",
  "scripts": {
    "lint": "eventvisor lint",
    "build": "eventvisor build",
    "test": "eventvisor test",
    "catalog": "eventvisor catalog export",
    "generate-code": "eventvisor generate-code --language typescript --out-dir src/generated"
  },
  "dependencies": {
    "@eventvisor/cli": "1.0.0"
  }
}
