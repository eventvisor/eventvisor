{
  "name": "@eventvisor/project-test-environments",
  "private": true,
  "version": "1.0.0",
  "description": "Eventvisor set testing examples for release environments",
  "scripts": {
    "lint": "eventvisor lint",
    "build": "eventvisor build",
    "test": "eventvisor test",
    "test:development": "eventvisor test --set=development",
    "test:staging": "eventvisor test --set=staging",
    "test:production": "eventvisor test --set=production"
  },
  "dependencies": {
    "@eventvisor/cli": "1.0.0"
  }
}
