{
  "name": "eventvisor-example-project",
  "version": "0.0.0",
  "private": true,
  "description": "Example Eventvisor project — copy this directory, npm install, then npx eventvisor lint && npx eventvisor test",
  "scripts": {
    "lint": "eventvisor lint",
    "test": "eventvisor test",
    "build": "eventvisor build",
    "catalog": "eventvisor catalog"
  },
  "devDependencies": {
    "@eventvisor/cli": "*"
  }
}
